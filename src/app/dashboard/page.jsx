export const metadata = {
  title: 'Dashboard',
  description: '...',
}

const DashboardPage = () => {
    return (
        <div>
            from dashboard
        </div>
    );
};

export default DashboardPage;